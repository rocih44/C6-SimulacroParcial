console.log("Prueba2");

const funciones = {
    'suma': (a,b)=> a+b,
    'resta': (a,b)=> a-b,
    'div': (a,b)=> a/b,
    'mult': (a,b)=> a*b,
    'resto': (a,b)=> a%b,
}

module.exports= funciones;

